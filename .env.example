# Required to generate a GPT-backed diagnosis and release report.
# Create an API key at https://platform.openai.com/api-keys.
OPENAI_API_KEY=

# Optional. Defaults to gpt-5.6 when omitted.
VERION_OPENAI_MODEL=gpt-5.6
